import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ru-mobile',
  webDir: '../../dist/apps/ru-mobile',
  bundledWebRuntime: false,
};

export default config;
